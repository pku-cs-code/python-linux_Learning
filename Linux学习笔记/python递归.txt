python递归

递归：
直接或间接调用自己
使用递归策略时，必须有一个明确的递归结束条件，称为递归出口
解题显得很简洁，但递归算法解题的运行效率较低，不提倡用递归算法设计程序
递归调用过程中系统为每一层的返回点、局部变量等开辟了栈来存储，递归次数过多容易造成栈溢出


要求：
每次调用在规模上都有所缩小（通常是减半）
相邻两次重复之间有紧密的联系，前一次要为后一次做准备
问题的规模极小时必须直接给出解答而不再进行递归调用，因而每次调用都是有条件的，
  无条件的递归将造成死循环而无法结束

